# Local segmentation models

Model binaries are downloaded locally and are intentionally excluded from Git.

The initial engine target is `u2net_human_seg.onnx`, sourced from the U²-Net human-segmentation model distribution used by rembg. It is an offline inference dependency, not an application asset.
